# yakuba

`yakuba` is a thin wrapper around `malevnc` for working with the Yakuba FlyEM
dataset in the same style as `malecns`.

The initial package surface focuses on:

- dataset switching helpers
- neuprint metadata and connectivity queries
- body id lookup helpers
- body annotation and skeleton convenience wrappers
