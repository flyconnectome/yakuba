% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xform.R
\name{xform_dyak2manc}
\alias{xform_dyak2manc}
\title{Transform yakuba neurons or points to MANC space (or back)}
\usage{
xform_dyak2manc(
  x,
  method = c("tps1000", "manual", "ngscene", "tps1000_unshifted"),
  units = c("nm", "microns"),
  inverse = FALSE,
  ...
)
}
\arguments{
\item{x}{An object compatible with \code{\link[nat:xform]{nat::xform()}} (neuron, neuronlist,
dotprops, surface, coordinate matrix etc).}

\item{method}{Which registration to use (see details).}

\item{units}{Coordinate units of \code{x} and of the returned object (in MANC
space unless \code{inverse = TRUE}).}

\item{inverse}{Whether to map from MANC to yakuba space.}

\item{...}{Additional arguments passed to \code{\link[nat:xform]{nat::xform()}}.

See the
\href{https://flyconnectome.github.io/yakuba/articles/yakuba-transforms.html}{transforms article}
for how the registrations and the offset were validated.}
}
\value{
A transformed object of the same kind as \code{x}, in the same \code{units}.
}
\description{
\code{xform_dyak2manc()} maps objects in yakuba space onto the MANC
template or, with \code{inverse = TRUE}, from MANC onto yakuba. Several
alternative registrations are available; only the default (\code{"tps1000"})
is used by \code{nat.templatebrains::xform_brain()}.
}
\details{
The available methods are:
\itemize{
\item \code{"tps1000"} (default): a thin-plate spline with 1000 landmarks computed
by Sebastian Cachero from a surface fitted to an earlier version of the
yakuba data. That surface is offset by a pure translation from current
yakuba coordinates, so points are first shifted by (-6.5, -7.1, -8.0) µm.
The shift was estimated by fitting that surface to
\link{yakuba_neuropil_shell} (precision ~1 µm); it reduces the mismatch between
matched yakuba and MANC descending neurons and neuropils by several µm.
\item \code{"tps1000_unshifted"}: the same registration without the shift, as used
by yakuba versions before the correction. Provided for comparison and
reproducibility; not recommended for new work.
\item \code{"manual"}: a thin-plate spline defined by 100 landmarks placed by hand
by Hiroshi Shiozaki.
\item \code{"ngscene"}: a thin-plate spline from yakuba to the male CNS
(\code{malecnsum}) defined by 21 landmark pairs from a clio neuroglancer scene,
followed by the \code{malecnsum} to \code{MANC} bridging registration. Requires the
suggested \code{malecns} package.
}

On descending neurons with a known MANC match (MDN, DNg13, DNa02) the
mean distance to the matched MANC neuron was 5.6, 6.4, 7.7 and 8.9 µm for
\code{"tps1000"}, \code{"manual"}, \code{"ngscene"} and \code{"tps1000_unshifted"},
respectively.

Inverse thin-plate splines are computed by swapping the landmark sets, so
round trips are approximate (~0.3 µm for \code{"tps1000"}, a few µm for
\code{"manual"}).

All registrations are defined in microns; nm inputs are scaled before and
after transformation. Requires the suggested \code{Morpho} package.
}
\examples{
\dontrun{
mdn <- read_dyak_neurons("MDN")
mdn.manc <- xform_dyak2manc(mdn)
mdn.manc2 <- xform_dyak2manc(mdn, method = "manual")
plot3d(mdn.manc, col = "black")
plot3d(mdn.manc2, col = "red")
}
}
\seealso{
\code{\link[=yakuba_register_xforms]{yakuba_register_xforms()}}
}
