% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xform.R
\name{dyak_lr_position}
\alias{dyak_lr_position}
\title{Calculate the left-right position wrt to the symmetrised yakuba midline}
\usage{
dyak_lr_position(x, units = c("nm", "microns"), ...)
}
\arguments{
\item{x}{An object containing XYZ vertex locations, compatible with
\code{\link[nat:xyzmatrix]{nat::xyzmatrix()}}.}

\item{units}{Units of \code{x} and of the returned displacement.}

\item{...}{Additional arguments passed to \code{\link[nat:xform]{nat::xform()}}.}
}
\value{
A numeric vector of point displacements (in \code{units}) where 0 is at
the midline and positive values are to the fly's right.
}
\description{
Returns the signed left-right position of points with respect
to the midline of the symmetric yakuba template, analogous to
\code{malevnc::manc_lr_position()}.
}
\details{
Points are mapped into \link{yakubasym} space and the X coordinate of
each point is compared with that of its mirror image. As in
\code{manc_lr_position()}, the returned value is this mirror displacement,
i.e. \emph{twice} the distance from the midline. Note that the X axis of the
yakuba dataset points to the fly's left (opposite to MANC); the sign is
adjusted so that positive values are on the fly's right in both packages.
}
\examples{
\dontrun{
dna02 <- read_dyak_neurons("DNa02")
# mean position of each neuron: positive values are on the fly's right
sapply(dna02, function(n) mean(dyak_lr_position(n)))
# colour points: red for left, green for right (nautical convention)
xyz <- nat::xyzmatrix(dna02[[1]])
points3d(xyz, col = ifelse(dyak_lr_position(xyz) < 0, "red", "green"))
}
}
\seealso{
\code{\link[=mirror_dyak]{mirror_dyak()}}
}
