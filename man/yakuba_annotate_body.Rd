% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bodies.R
\name{yakuba_annotate_body}
\alias{yakuba_annotate_body}
\title{Set Clio body annotations}
\usage{
yakuba_annotate_body(
  x,
  test = TRUE,
  version = NULL,
  write_empty_fields = FALSE,
  designated_user = NULL,
  protect = c("user"),
  chunksize = 50,
  check_types = TRUE,
  ...,
  dataset = dyak_default_dataset()
)
}
\arguments{
\item{x}{A data frame, list or JSON string containing body annotations. End
users are strongly recommended to use data frames.}

\item{test}{Whether to use the test Clio store (recommended until you are
sure that you know what you are doing).}

\item{version}{Optional Clio version to associate with this annotation. The
default \code{NULL} uses the current version returned by the API.}

\item{write_empty_fields}{When \code{x} is a data frame, this controls whether
empty fields in \code{x} overwrite fields in the Clio database.}

\item{designated_user}{Optional alternate user recorded in Clio for the
annotation.}

\item{protect}{Vector of fields that will not be overwritten if they already
have a value in Clio. Set to \code{TRUE} to protect all fields and to \code{FALSE} to
overwrite all fields for which you provide data.}

\item{chunksize}{When you have many bodies to annotate the request will by
default be sent 50 records at a time to avoid timeouts. Set to \code{Inf} to
insist that all records are sent in a single request.}

\item{check_types}{Whether to verify data-frame column types against the
active yakuba Clio schema.}

\item{...}{Additional parameters passed to \code{\link[pbapply:pbapply]{pbapply::pbsapply()}}.}

\item{dataset}{The name of the dataset as reported in Clio or neuprint.}
}
\value{
\code{NULL} invisibly on success. Errors out on failure.
}
\description{
Set Clio body annotations
}
\details{
Details of the Clio body annotation system are provided in
\code{\link[malevnc:manc_annotate_body]{malevnc::manc_annotate_body()}}. It can take some time to apply
annotations, so requests are chunked by default in groups of 50.

A single column called \code{position} or 3 columns named \code{x}, \code{y}, \code{z} or \code{X},
\code{Y}, \code{Z} in any form accepted by \code{\link[nat:xyzmatrix]{nat::xyzmatrix()}} will be converted to a
position stored with each record. This is recommended when creating
records.

When \code{protect=TRUE} no data in Clio will be overwritten, only new data
will be added. When \code{protect=FALSE} all fields will be overwritten by new
data for each non-empty value in \code{x}. If \code{write_empty_fields=TRUE} then
even empty fields in \code{x} will overwrite fields in the database.
}
\section{Validation}{

Validation depends on how you provide your input data. If \code{x} is a data
frame then each row is checked for some basics including the presence of a
\code{bodyid}, and empty fields are removed. We also check field types against
the active yakuba Clio schema by default.
}

\section{Users}{

Clio records a user and timestamp for every modification to fields in a
record. By default your email address from the Clio token will be used. You
can specify an alternate user with the \code{designated_user} argument; see
\code{\link[malevnc:manc_annotate_body]{malevnc::manc_annotate_body()}} for details.
}

\examples{
\donttest{
ids <- dyak_ids("DNa02")
yakuba_annotate_body(
  data.frame(bodyid = ids[1], group = ids[1]),
  test = TRUE
)
}
}
\seealso{
\code{\link[malevnc:manc_annotate_body]{malevnc::manc_annotate_body()}}
}
