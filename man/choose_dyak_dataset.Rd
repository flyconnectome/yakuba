% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urls.R
\name{choose_dyak_dataset}
\alias{choose_dyak_dataset}
\title{Switch the default dataset for \verb{dyak_*} functions}
\usage{
choose_dyak_dataset(dataset = "yakuba")
}
\arguments{
\item{dataset}{The name of the dataset as reported in Clio (usually
\code{yakuba}), or neuprint (\code{yakuba-vnc}). These are aliases for the in
progress production yakuba dataset. Currently there are no released
datasets, but we expect those to look like e.g. \code{yakuba-vnc:v0.1}.}
}
\value{
A named list of previous option values, matching \code{options()}.
}
\description{
\code{choose_dyak_dataset()} sets the default dataset used by all
\verb{dyak_*} functions. It is the recommended way to access yakuba dataset
variants. Unlike \code{\link[=choose_dyak]{choose_dyak()}} it does \emph{not} permanently change the
default dataset used when callers use functions from the \code{malevnc} package
directly (such as \code{malevnc::manc_xyz2bodyid()}). Known aliases for the main
yakuba dataset do not require a live Clio lookup.
}
\examples{
\donttest{
choose_dyak_dataset("yakuba")
dyak_ids("DNa02")
}
}
