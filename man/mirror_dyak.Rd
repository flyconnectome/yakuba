% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xform.R
\name{mirror_dyak}
\alias{mirror_dyak}
\alias{symmetric_dyak}
\title{Mirror yakuba neurons or points to the opposite side of the VNC}
\usage{
mirror_dyak(x, units = c("nm", "microns"), subset = NULL, ...)

symmetric_dyak(
  x,
  units = c("nm", "microns"),
  mirror = FALSE,
  subset = NULL,
  ...
)
}
\arguments{
\item{x}{An object compatible with \code{\link[nat:xform]{nat::xform()}} (neuron, neuronlist,
dotprops, surface, coordinate matrix etc).}

\item{units}{Coordinate units of \code{x} and the returned object. Defaults to
nm, matching \code{\link[=read_dyak_neurons]{read_dyak_neurons()}}.}

\item{subset}{Optional subset passed to \code{\link[nat:xform]{nat::xform()}}, e.g. when
transforming selected elements of a neuronlist.}

\item{...}{Additional arguments passed to \code{\link[nat:xform]{nat::xform()}}.}

\item{mirror}{Whether to mirror across the midline of the symmetric space.}
}
\value{
A transformed object of the same kind as \code{x}, in the same \code{units}.
}
\description{
\code{mirror_dyak()} mirrors neurons, surfaces and other point data
in yakuba space to the opposite side of the VNC.

\code{symmetric_dyak()} transforms objects onto a symmetrised version of the
yakuba template, optionally mirroring across the midline.
}
\details{
Mirroring maps \code{x} into the symmetric \link{yakubasym} space with a
bundled thin-plate spline registration (\code{yakuba_yakubaSym_1000pts_tps.rds},
computed by Sebastian Cachero), flips across the X axis and then maps back
with the inverse registration. The registration is defined in microns;
nm inputs are scaled before and after transformation. Requires the
suggested \code{Morpho} package.

Denser versions of the registration (10000 points) were also computed but
gave < 1 µm change in mirrored positions at ~500x the compute cost.

\code{symmetric_dyak()} returns objects in \link{yakubasym} space, using
\code{units} for both input and output.
}
\examples{
\dontrun{
dna02 <- read_dyak_neurons("DNa02")
dna02.m <- mirror_dyak(dna02)
plot3d(dna02, col = "black")
plot3d(dna02.m, col = "red")
}
\dontrun{
# DNa02 neurons and their mirror images in symmetric space
dna02.sym <- symmetric_dyak(dna02)
dna02.sym.m <- symmetric_dyak(dna02, mirror = TRUE)
}
}
\seealso{
\link{yakubasym}
}
